<%= form_with(model: sy_config, local: true) do |form| %>
  <% if sy_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sy_config.errors.count, "error") %> prohibited this sy_config from being saved:</h2>

      <ul>
      <% sy_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :var %>
    <%= form.text_field :var %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
