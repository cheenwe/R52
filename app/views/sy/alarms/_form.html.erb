<%= form_with(model: sy_alarm, local: true) do |form| %>
  <% if sy_alarm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sy_alarm.errors.count, "error") %> prohibited this sy_alarm from being saved:</h2>

      <ul>
      <% sy_alarm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.number_field :service_id %>
  </div>

  <div class="field">
    <%= form.label :alarm_at %>
    <%= form.datetime_select :alarm_at %>
  </div>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_field :reason %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
