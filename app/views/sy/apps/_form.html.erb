<%= form_with(model: sy_app, local: true) do |form| %>
  <% if sy_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sy_app.errors.count, "error") %> prohibited this sy_app from being saved:</h2>

      <ul>
      <% sy_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :abbr %>
    <%= form.text_field :abbr, disabled:true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
