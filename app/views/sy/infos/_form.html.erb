<%= form_with(model: sy_info, local: true) do |form| %>
  <% if sy_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sy_info.errors.count, "error") %> prohibited this sy_info from being saved:</h2>

      <ul>
      <% sy_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.number_field :service_id %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.number_field :state %>
  </div>

  <div class="field">
    <%= form.label :send_at %>
    <%= form.datetime_select :send_at %>
  </div>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_field :reason %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
